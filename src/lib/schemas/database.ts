/**
 * Tipos de la base de datos Supabase.
 *
 * D1: placeholder. Cuando el proyecto Supabase esté linkeado, regenerar con:
 *   npx supabase gen types typescript --project-id blmiebnnprwaupyatsyb > src/lib/schemas/database.ts
 * (eso reemplaza este archivo con los tipos reales de tablas/vistas/funciones).
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
